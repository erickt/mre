#[link(name = "mre",
       vers = "0.1pre",
       uuid = "b85803ee-819d-4b9d-adcd-ed8e356ceee3")];
#[crate_type = "lib"];

use std;
use mongrel2;
use mustache;
use pcre;
use zmq;

mod response;
mod router;
